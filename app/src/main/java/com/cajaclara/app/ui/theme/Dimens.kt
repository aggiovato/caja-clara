package com.cajaclara.app.ui.theme

import androidx.compose.ui.unit.dp

/**
 * App-wide dimension tokens. Change here to restyle everywhere.
 *
 * [AppBorderWidth] matches the search input's outline so cards and inputs share the same
 * border thickness.
 */
val AppBorderWidth = 2.dp
